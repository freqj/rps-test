spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:report_service}
spring.datasource.username=${DB_USER:report_service}
spring.datasource.password=${DB_PASSWORD:report_service}
