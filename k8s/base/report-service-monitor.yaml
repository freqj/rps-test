apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: report-service
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - rps-test

  selector:
    matchLabels:
      app: report-service

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s